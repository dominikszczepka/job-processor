import FieldBase from "./FieldBase";

class PossibleNightWorkField implements FieldBase {
    value = `"possible_night_work": null // boolean`;
    array = false;
    nested = false;
  }
export default PossibleNightWorkField; 